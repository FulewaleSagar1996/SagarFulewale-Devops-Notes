COMMANDS IN DOCKER
** To see all images present in your local = docker images

** To find out images in docker hub = docker search jenkins(image name)

** To download image from docker hub to local machine = docker pull jenkins(image name)

** To give name to container =  docker run -it --name bhupinder ubuntu /bin/bash

** To check, service is start or not = service docker status or docker info

** To start container = docker start bhupinder (conatiner name)

** To go inside the container = docker attach container name

** To see all containers run or stop= docker ps -a

** To see running containers = docker ps (process staus)

** To stop containers = docker stop containername

** To delete container = docker rm dockername
docker image rm imagename ----------- to dlt images 



